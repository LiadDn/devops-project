name: myapp
on:
  push:
    branches: [master]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      - name: Build Image
        run: docker build -t myapp:latest .
      - name: Push Image
        run: docker push myapp
